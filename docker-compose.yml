services:
  postgres:
    image: postgres:15-alpine
    container_name: f1-postgres-dev
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: f1_pool
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
  